# EliteTrades

Premium Trading Landing Page with advanced aesthetics and multi-payment integration.

## Project Structure
This repository contains the EliteTrades project located in the `project` directory.

## Getting Started
To run the project:
1. Navigate to the `project` directory.
2. Run `npm install`.
3. Run `npm run dev`.
